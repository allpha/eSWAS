@model Swas.Client.Models.RegionViewModel

@{
    ViewBag.Title = "რეგიონის რედაქტირება";
}

<div class="widget-body" style="margin-top: -10px;">
    <div id="fontawesome-container">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-horizontal">
                <h4>ჩანაწერის რედაქტირება</h4>
                <hr />
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                @Html.HiddenFor(model => model.Id)

                <div class="form-group">
                    @Html.LabelFor(model => model.Name, htmlAttributes: new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" value="შენახვა" class="btn btn-primary" />
                        <a class="btn btn-primary" href="/Region/Index">გაუქმება</a>
                    </div>
                </div>
            </div>
        }

    </div>
</div>
