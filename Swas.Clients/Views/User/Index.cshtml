@using Swas.Clients.Common
@{
    ViewBag.Title = "Index";
}


<div class="row">
    <div class="col-md-12 ">
        <!-- BEGIN SAMPLE FORM PORTLET-->
        <div class="portlet light bordered" id="viewBody">
            <div class="portlet-title">
                <div class="caption caption-md">
                    <i class="icon-pointer font-blue-madison"></i>
                    <span class="caption-subject font-blue-madison bold uppercase" style="font-family: mtavrulibold">მომხმარებლები</span>
                </div>
                <div class="btn-group pull-right">
                    @if (Globals.SessionContext.HasPermission("SolidWasteAct.Create"))
                    {
                        <button style="margin-right:5px" class="btn blue-madison btn-outline" onclick="javascript:create();">
                            <i class="fa fa-plus"></i>
                            ახალი
                        </button>
                    }
                </div>
            </div>

            <a id="ajax-model-dialog-editor" data-url="/home/test" data-toggle="modal"> </a>
            <div id="ajax-modal" class="modal fade" data-width="800" tabindex="-1"> </div>

            <div class="portlet-body">

                @if (Globals.SessionContext.HasPermission("User.Delete"))
                {
                    <input type="hidden" id="hasDelete" value="true" />
                }
                else
                {
                    <input type="hidden" id="hasDelete" value="false" />
                }
                @if (Globals.SessionContext.HasPermission("User.Edit"))
                {
                    <input type="hidden" id="hasEdit" value="true" />
                }
                else
                {
                    <input type="hidden" id="hasEdit" value="false" />
                }
                @if (Globals.SessionContext.HasPermission("User.Unlock"))
                {
                    <input type="hidden" id="hasUnlock" value="true" />
                }
                else
                {
                    <input type="hidden" id="hasUnlock" value="false" />
                }
                @if (Globals.SessionContext.HasPermission("User.Disible"))
                {
                    <input type="hidden" id="hasDisible" value="true" />
                }
                else
                {
                    <input type="hidden" id="hasDisible" value="false" />
                }
                @if (Globals.SessionContext.HasPermission("User.ChangePassword"))
                {
                    <input type="hidden" id="hasChangePassword" value="true" />
                }
                else
                {
                    <input type="hidden" id="hasChangePassword" value="false" />
                }

                <div class="table-scrollable table-scrollable-borderless">
                    <table class="table table-hover table-light" id="itemSource">
                        <thead>
                            <tr class="uppercase">
                                <th style="font-family: mtavrulibold"> სახელი, გვარი</th>
                                <th style="font-family: mtavrulibold"> პირადი #</th>
                                <th style="font-family: mtavrulibold"> მომხმარებლის სახელი</th>
                                <th style="font-family: mtavrulibold"> ელ.ფოსტა</th>
                                <th style="font-family: mtavrulibold"> სამუშაო პოზიცია</th>
                                <th style="font-family: mtavrulibold"> მომხმარებელთა ჯგუფი</th>
                                <th style="font-family: mtavrulibold"> სტატუსი</th>
                                <th width="205px">
                                </th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

</div>
@section PageScripts{
    <script src="~/assets/script/BusyIndicator/BusyIndicator.js"></script>
    <script src="~/assets/script/User/Index.js"></script>
}


